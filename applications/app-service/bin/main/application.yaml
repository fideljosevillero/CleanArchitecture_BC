server:
  port: 8080
spring:
  application:
    name: "retoBC"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
adapter:
  restconsumer:
    url: "http://localhost:8080"
routes:
  uri_balanceAndMovement: balance-and-details